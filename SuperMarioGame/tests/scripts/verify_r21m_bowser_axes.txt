# R21 Lane M — Bowser's bridge now takes THREE axes on HARD, and the bridge only
# falls when the last one is reached. Observed in the running game (directives
# 9 & 10).
#
# What this proves, in order:
#   0. The arena, entered from the left enclosure post.
#   1. The first axe is taken and the HUD reads AXES 1/3 — the bridge is still
#      there and Bowser is still alive.
#   2. The second axe: AXES 2/3, bridge STILL there and Bowser STILL at full
#      health. This is the whole behavioural change — before it, the very first
#      axe dropped the bridge and ended the fight.
#
# What it does NOT show, stated plainly: the third axe being reached and the
# bridge dropping. That axe stands on the far ledge behind the arena's
# right-hand enclosure post at (189,19), and no scripted walk got to it — plain
# walking stalls against the post, and a jump big enough to clear the post also
# sails over the axe. The chop itself is observed in the EASY companion run,
# tests/scripts/verify_r21m_bowser_axe_easy_chop.txt, where one axe is the whole
# roster and reaching it IS reaching the last one.
#
# PRECONDITIONS, all three temporary and all three restored afterwards:
#   * saves/config.json  "difficulty": "hard"  (3 axes) and "debugMode": true
#     (F3 INVINCIBLE, so Bowser's fire cannot end the run in the middle of the
#     observation — the axes are what is being watched here, not the fight).
#   * saves/progress.json with levels 0 and 1 completed, so CampaignProgress
#     unlocks 1-3 and the world map opens on it: START GAME -> confirm
#     character -> confirm node lands in Bowser's castle.
#   * level_3.json "spawnPoint" overridden to (180,18) — standing on the LEFT
#     enclosure post of the arena, so the sweep runs west to east across all
#     three axes and the last one is reached from the far ledge, which is solid
#     ground rather than the bridge that is about to go. The same sanctioned trick
#     tests/scripts/verify_r12_bridge_axe.txt used and reverted; walking the
#     178 tiles from the real spawn is not something a script can do reliably.
#     Reverted with `git checkout` and confirmed with `git diff` after the run.
#
# Run with (from SuperMarioGame/):
#   ./build_lane_m/SuperMarioGame --script tests/scripts/verify_r21m_bowser_axes.txt
#
# Screenshots land in saves/shots/.
#
# NOTE on `hold`: InputScript::update queues the release and drains the next
# step on the same frame (InputScript.cpp:212), so a `hold` does NOT block. Each
# `hold` below is followed by a `wait` long enough for the walk it starts to
# finish before the shot is taken.

wait 2.5
press Enter
wait 2.0
press Enter
wait 2.5
# The world map cursor does NOT open on the furthest unlocked node in this
# build (verify_r12_bridge_axe.txt's comment says it does; it opened on 1-1
# here and loaded the wrong level), so walk it to node 3 explicitly.
press Right
wait 0.4
press Right
wait 0.6
press Enter
wait 3.0
press F3
wait 0.5
shot r21m_a_00_arena_left_post_three_axes

# Walk EAST across the bridge, WITHOUT jumping at first. The axes sit at
# standing height on the bridge deck, and bunny-hopping sails straight over
# them — an attempt that jumped from the start collected one axe instead of two.
# Jump taps come only at the end, to try the right-hand enclosure post.
#
# Jump is W, not Up: W/A/D is Player 1's binding set and Up belongs to Player 2
# (saves/config.json keyBindings, and the multiplayer page prints both sets).
# Two earlier attempts used Up here and Mario stood against the enclosure post
# for the whole run — the hops were going to a player who was not in the level.
#
# INVINCIBLE (F3) is on so Bowser's fire cannot end the run in the middle of
# the observation; the axe route is what is being watched, not the fight.
#
# `hold` does not block (InputScript.cpp:212), so this is one continuous walk
# east with jump taps interleaved to clear both enclosure posts.
hold D 40.0
wait 2.0
shot r21m_a_01_axes_1_of_3
wait 2.5
shot r21m_a_02_axes_2_of_3
press W
wait 0.9
press W
wait 0.9
press W
wait 0.9
press W
wait 0.9
press W
wait 0.9
press W
wait 0.9
shot r21m_a_03_at_the_far_post
wait 2.5
shot r21m_a_04_end_of_run
quit
