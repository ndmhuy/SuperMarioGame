# R21 Lane E - the level editor, driven end to end from the main menu.
#
# Uses InputScript's synthetic pointer (mouse / mousedown / mouseup) and its
# modifier support (press Z ctrl), both added for this pass: the editor is the
# only mouse-driven screen in the game, and its Ctrl+Z / Ctrl+S shortcuts could
# not otherwise be exercised by a script at all.
#
# Canvas geometry: the canvas is screen (300,24)-(980,560) at 1 world pixel per
# screen pixel, framed on the BOTTOM-left of the level. For the blank 60x22
# level this screen opens with, world (0,168) sits at screen (300,24), so
# screen = (300 + 32*tileX, 24 + 32*tileY - 168).
#
#   ./build_r21_lane_e/SuperMarioGame --script tests/scripts/verify_r21e_editor.txt

wait 1.5
shot r21e_00_menu

# MAP EDITOR is the fifth main-menu row.
press Down
press Down
press Down
press Down
wait 0.3
press Enter
wait 1.5
shot r21e_01_editor_blank

# --- Rectangle fill: a floor from tile (0,18) to (19,21), ONE undo step --
press R
wait 0.3
mouse 316 448
wait 0.2
mousedown left
wait 0.3
mouse 924 544
wait 0.3
mouseup left
wait 0.5
shot r21e_02_rect_fill

# --- The three types that placed NOTHING before this change -------------
# The Select tool flips the palette to the entity layer; B returns to Paint.
press V
wait 0.2
press B
wait 0.2
# Coin is the default. Q steps back 13 entries to Goomba, the first enemy.
press Q
press Q
press Q
press Q
press Q
press Q
press Q
press Q
press Q
press Q
press Q
press Q
press Q
wait 0.3
mouse 444 416
wait 0.15
mousedown left
wait 0.2
mouseup left
wait 0.4
shot r21e_03_goomba

# Question block: 27 entries forward from Goomba.
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
press T
wait 0.3
mouse 572 320
wait 0.15
mousedown left
wait 0.2
mouseup left
wait 0.4
shot r21e_04_question_block

# Pipe: three more (hidden block, ice block, pipe).
press T
press T
press T
wait 0.3
mouse 732 320
wait 0.15
mousedown left
wait 0.2
mouseup left
wait 0.6
shot r21e_05_pipe

# --- Undo / redo from the KEYBOARD. Ctrl+Z did nothing at all before. ---
press Z ctrl
wait 0.5
shot r21e_06_after_undo_pipe_gone
press Z ctrl shift
wait 0.5
shot r21e_07_after_redo_pipe_back

# --- Select / Inspector: the pipe's warp fields, unauthorable until now --
press V
wait 0.2
mouse 732 352
wait 0.15
mousedown left
wait 0.2
mouseup left
wait 0.4
shot r21e_08_inspector_pipe

# --- Spawn point tool ---------------------------------------------------
press P
wait 0.2
mouse 380 416
wait 0.15
mousedown left
wait 0.2
mouseup left
wait 0.4
shot r21e_09_spawn_point

# --- Ctrl+S on a never-saved level offers Save As, with the exact path ---
press S ctrl
wait 1.0
shot r21e_10a_save_as_dialog
press Escape
wait 0.5
quit
