# Archive — what moved here, from where, and why

**What this is**: an index of files that were superseded, scratch, or stale
renders and moved out of the active tree rather than deleted (g-rule-15: "One
Current Doc — Legacy Goes to the Archive" — never delete, move it here with a
dated prefix and say why). **Who it is for**: anyone who follows a stale link
to one of these files, or who wonders why a path referenced in an old log
entry or commit message no longer exists at the repo root.

Everything below moved on **2026-09-02** as Phase 1 of the pre-submission
sweep (`docs/issues/submission_sweep_plan_2026-09-02.md` §5.1). Nothing was
deleted — every file here is a straight `git mv`, so its full history is
still `git log --follow`-able from its new path.

---

## Scratch files from the class-diagram HTML experiments

| File | Moved from | Last touched | Why |
| :--- | :--- | :--- | :--- |
| `2026-09-02_test-1.svg` … `2026-09-02_test-6.svg` | repo root | 2026-06-20 | Scratch SVG exports from early Mermaid class-diagram rendering experiments. Referenced only as historical evidence in `logs/agent_history.log` (a later session used them to confirm what an ambiguous user request meant); no live document points at them. |
| `2026-09-02_test_escaping.html` | repo root | 2026-06-20 | Scratch HTML from the same experiments, for testing character-escaping in a generated diagram page. |
| `2026-09-02_scratch_all_frame_keys.txt` | repo root | 2026-08-11 | Scratch dump of sprite animation frame keys, used once to cross-check `entities.json` and never referenced again. |

## Hand-drawn diagrams superseded by the generator script

| File | Moved from | Last touched | Why |
| :--- | :--- | :--- | :--- |
| `2026-09-02_class_diagram.html` | repo root | 2026-06-19 | Hand-maintained HTML class diagram. Superseded by `tools/gen_class_diagram.py`, which now generates `class_diagram.md` from the actual source tree (g-rule-22: a diagram restating code facts must be generated, not hand-synced) — `class_diagram.md` itself is **not** archived, it is current and stays at the repo root. |
| `2026-09-02_class_diagram_slides.pdf` | repo root | 2026-06-20 | PDF render of the above, same supersession. |
| `2026-09-02_SuperMarioGame_ClassDiagram.pdf` | repo root | 2026-06-20 | Hand-drawn UML export, superseded by the generated diagram. |
| `2026-09-02_SuperMarioGame_ClassDiagram_Horizontal.pdf` | repo root | 2026-06-20 | Landscape variant of the same, same supersession. |

## Superseded planning / proposal documents

| File | Moved from | Last touched | Why |
| :--- | :--- | :--- | :--- |
| `2026-09-02_25125083.md` | repo root | 2026-07-11 (content dated 2026-06-03) | One-time consolidated document (student-ID-named) generated by `scripts/combine_docs.py` for a single TA proposal submission, combining `FEATURE_PROPOSAL.md`, `CS202_FinalProject_SuperMario_Spec.md`, `SPEC.md`, `implementation_plan.md`, `TASKS.md` and `AGENTS.md` behind a custom intro. It is a frozen snapshot of those files' June content and is not maintained; `scripts/combine_docs.py` carries a comment as of this sweep explaining it should not be blindly re-run (see below). |
| `2026-09-02_two_member_workflow.md` | repo root | 2026-06-09 | Early two-member Git workflow proposal, superseded by the branch-isolation and member-recognition rules now in `AGENTS.md`. |
| `2026-09-02_sprites_list.md` | repo root | 2026-08-03 | Sprite/animation sourcing checklist. All listed sheets are already sourced and integrated into `SuperMarioGame/assets/`; `docs/SPRITE_AGENT.md` now points here for reference rather than treating it as a live spec. |
| `2026-09-02_implementation_plan.md` | repo root | see note | See "`implementation_plan.md` — a special case" below. |

**Not moved, despite matching this category on paper**: `docs/two_player_ai_plan.md`.
The sweep plan listed it alongside the closed audit/fix-plan documents below,
but it is actively cited **by section number** (§2A, §2B, §3.2, §6) as design
rationale for tuned constants in seven live source files —
`SuperMarioGame/include/Core/GameMode.hpp`, `DevPanel.hpp`,
`SuperMarioGame/include/Entities/HeuristicPolicy.hpp`, `AIController.hpp`,
`IAIPolicy.hpp`, `ShadowMario.hpp`, and
`SuperMarioGame/tests/verify_multiplayer_ai.cpp`. Archiving it would orphan
those comments' path references, and fixing seven engine-source comments is
outside this lane's scope (no C++ source was touched by this sweep). Left in
place; flagged for the user to decide.

### `implementation_plan.md` — a special case

Unlike the other files above, `implementation_plan.md` was not simply
superseded — its content had been **overwritten with something unrelated**.
`SPEC.md`, `AGENTS.md`, `README.md` and `25125083.md` all still described it
as *"architecture diagrams and user answers"* (the file `SPEC.md` was
originally compiled from, per its own provenance line), but by 2026-09-02 the
file actually held a design doc for an unrelated "Standalone Enemy Behavior
Test Suite" — the description everywhere else had gone stale without anyone
noticing, because nothing re-reads a plan file to check it still matches its
own citation.

Decision (user-confirmed, plan §7 Q1): archive it, and repoint the
architecture/behaviour source of truth at `SPEC.md`, which was already the
Key Files list's first entry. `AGENTS.md`'s Key Files pointer (line 20,
project-owned, above the AgentHub block) and `README.md`'s project-structure
tree were updated to drop the stale `implementation_plan.md` entry and name
`SPEC.md` instead.

**Correction (2026-09-02, same day, coordinator-caught):** this lane's first
pass also edited g-rule-6's text inside `AGENTS.md` — both the BOOT DIGEST
one-liner and the full Tier-2 rule — to drop `implementation_plan.md` from
its plan-file list. That was wrong: g-rule-6 lives inside the
`<!-- AGENTHUB:L3:BEGIN/END -->` generated block (lines 396-612), a Layer 3
rule owned by AgentHub, not this project (g-rule-18: "Suggest Rules
Upstream, Do Not Fork Them"). Editing it here would have silently reverted
on the next AgentHub sync — `AGENTS.md` would go back to citing the archived
file with nothing failing to say so. Both edits were reverted byte-for-byte
against `dev @ 6af4f8e` (verified with `git diff dev -- AGENTS.md`: the only
remaining hunks are the line-20 Key Files pointer and the structure-tree
row, both above line 396). The suggestion was instead routed upstream via
`.agenthub/suggestions.json`, per the "Suggesting a rule back to AgentHub"
section of `AGENTS.md` (around line 459) — proposing g-rule-6 either drop
the file-specific example (a per-project fact that already drifted once) or
read cleanly as "(SPEC.md or TASKS.md)" if an example stays inline, avoiding
the comma-before-or artifact a naive deletion leaves. Promoting that
suggestion is a human/AgentHub-maintainer decision (g-rule-18); this lane
does not have or use write access to `~/Documents/AgentHub`.

**Net effect**: in this project's own tree, g-rule-6's generated text still
literally names `implementation_plan.md` as a plan file, even though that
file is now archived — this is a known, upstream-flagged staleness, not an
oversight. `SPEC.md` itself was **not** touched — it is frozen ("APPROVED —
do not modify without user confirmation") and its provenance line is a
historical claim about how it was produced, which stays true regardless of
what the source file holds today. `25125083.md`'s references to
`implementation_plan.md` are inside an already-archived, dated snapshot and
were likewise left as written.

## Closed audits and fix-plans

| File | Moved from | Why |
| :--- | :--- | :--- |
| `2026-09-02_code_audit_2026-08-18.md` | `docs/issues/` | 38 findings, 37 resolved + 1 retracted (per `completion_plan.md`). All items closed. |
| `2026-09-02_member_a_fix_plan.md` | `docs/issues/` | All 12 work packages complete. |
| `2026-09-02_member_b_input_sync.md` | `docs/issues/` | Superseded input-rebinding fix plan; tracked issue (#9) resolved. |
| `2026-09-02_completion_plan.md` | `docs/issues/` | All four tiers complete. |

These four still link to each other (the same-directory relative links were
updated to the new `2026-09-02_`-prefixed filenames) and to source files two
levels up (`../../SuperMarioGame/...`, unaffected — `docs/archive/` sits at
the same depth as `docs/issues/` did). `docs/issues/spec_feature_audit_2026-08-31.md`
cites all four by their old paths in several places and **stays in
`docs/issues/`** (it is not closed); rather than rewrite every inline
citation, it now carries a one-line archive note near the top pointing here.

**Not updated** (out of this lane's scope — report generation is owned by a
different phase of the sweep): the same four paths are also cited from
`submission_documents/Group52_SuperMarioGame_Report.md`,
`reports/report_content.py`, `reports/Group52_SuperMarioGame_FinalReport.html`,
`reports/artifact_final_report.html`, and a comment in
`reports/html_to_latex.py`. These are prose citations of an audit's location
inside generated/authored report content, not functional links — flagged for
whoever next touches the report pipeline.

## Build outputs, untracked rather than archived

Per g-rule-19 ("Build Outputs Are Built, Never Committed"), these were
`git rm --cached` and added to `.gitignore` instead of moved — the source
they were rendered or zipped from stays fully tracked, so nothing is lost by
removing the derived copy from git:

| File(s) | Was a build output of |
| :--- | :--- |
| `TASKS.pdf`, `TASK_DIVISION.pdf`, `SPEC.pdf`, `implementation_plan.pdf` | Stale PDF renders of the tracked `.md` files of the same name, made by `scripts/generate_pdf.py`. |
| `submission_documents.zip` | The tracked `submission_documents/` directory. Rebuilt in Phase 7. |
| `docs/Group52_04.zip`, `_05.zip`, `_09.zip`, `_10.zip` | The tracked `docs/Group52_0N/` weekly report directories. |

All of these remain on disk locally (git only stopped tracking them); they
are not deleted and can be regenerated on demand.

## Left untracked, not archived (not part of the submission tree)

`SuperMarioGame/corpus/`, `saves/ai/`, `saves/eval/` are untracked side-project
data for the map-generation/RL work and were already covered by existing
`.gitignore` rules before this sweep — no action was needed, listed here only
so this README is a complete account of §5.1.

## What this does NOT cover

`saves/profile.json` and `saves/config.json` are per-installation runtime
data, not documents — handled separately (see `logs/agent_history.log` for
2026-09-02, "Phase 1 — Archive older artifacts" entry) and not part of this
archive directory.
